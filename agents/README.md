# Agents

AI agent definitions installed as symlinks into `~/.claude/agents/` and `~/.copilot/agents/`.

Each agent is a markdown file that defines a specialized AI agent with a specific role, responsibilities, and behaviour guidelines.

No agents are currently defined. Add agent definition files to this directory and re-run `install.sh` to install them.
